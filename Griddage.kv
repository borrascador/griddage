# Griddage.kv

<BoardDisplay>:
	rows: 5
	canvas:
		Color:
			rgb: 0, 0.7, 0
		Rectangle:
			pos: self.pos
			size: self.size

	BoxLayout:
		Image:
			source: 'cards/' + root.grid[(1,1)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(1,1)
		Image:
			source: 'cards/' + root.grid[(2,1)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(2,1)		
		Image:
			source: 'cards/' + root.grid[(3,1)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(3,1)		
		Image:
			source: 'cards/' + root.grid[(4,1)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(4,1)		
		Image:
			source: 'cards/' + root.grid[(5,1)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(5,1)
			
	BoxLayout:
		Image:
			source: 'cards/' + root.grid[(1,2)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(1,2)
		Image:
			source: 'cards/' + root.grid[(2,2)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(2,2)		
		Image:
			source: 'cards/' + root.grid[(3,2)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(3,2)		
		Image:
			source: 'cards/' + root.grid[(4,2)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(4,2)		
		Image:
			source: 'cards/' + root.grid[(5,2)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(5,2)
			
	BoxLayout:
		Image:
			source: 'cards/' + root.grid[(1,3)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(1,3)
		Image:
			source: 'cards/' + root.grid[(2,3)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(2,3)		
		Image:
			source: 'cards/' + root.grid[(3,3)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(3,3)		
		Image:
			source: 'cards/' + root.grid[(4,3)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(4,3)		
		Image:
			source: 'cards/' + root.grid[(5,3)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(5,3)
			
	BoxLayout:
		Image:
			source: 'cards/' + root.grid[(1,4)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(1,4)
		Image:
			source: 'cards/' + root.grid[(2,4)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(2,4)		
		Image:
			source: 'cards/' + root.grid[(3,4)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(3,4)		
		Image:
			source: 'cards/' + root.grid[(4,4)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(4,4)		
		Image:
			source: 'cards/' + root.grid[(5,4)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(5,4)
			
	BoxLayout:
		Image:
			source: 'cards/' + root.grid[(1,5)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(1,5)
		Image:
			source: 'cards/' + root.grid[(2,5)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(2,5)		
		Image:
			source: 'cards/' + root.grid[(3,5)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(3,5)		
		Image:
			source: 'cards/' + root.grid[(4,5)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(4,5)		
		Image:
			source: 'cards/' + root.grid[(5,5)] + '.png'
			on_touch_down: if self.collide_point(*args[1].pos): root.flip_card(5,5)